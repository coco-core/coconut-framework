import {
  expectInOrder,
  checkClassMetadataAsExpected
} from './decorator';

import {
  getMetadata,
  getAllMetadata,
} from "../ioc-container/metadata-runtime-config.ts";


export {
  expectInOrder,
  checkClassMetadataAsExpected,
  getMetadata,
  getAllMetadata,
}